# CLAUDE.md — jump-section AI Context

이 문서는 **규칙만** 담는다. 코드에서 읽을 수 있는 것(API 목록, 디렉터리 구조, 테스트 헬퍼)은 넣지
않는다 — 매 실행마다 읽히므로 길어지면 비용이고, 사본은 반드시 원본과 어긋난다.

## 프로젝트 개요

**jump-section**은 메뉴 클릭 시 해당 섹션으로 부드럽게 스크롤하고, 스크롤 위치에 따라 활성 섹션을
추적하는 TypeScript 라이브러리다.

- **저장소**: https://github.com/bae080311/jump-section
- **npm**: `@jump-section/core`, `/react`, `/vue`, `/svelte`
- **패키지 매니저**: pnpm + turbo (모노레포)

## 핵심 아키텍처

`ScrollManager`(`packages/core/src/ScrollManager.ts`)가 모든 기능의 중심이고, 프레임워크
패키지(react/vue/svelte)는 이를 래핑한 얇은 어댑터다. **Public API는 그 파일에서 직접 읽는다** —
여기에 목록을 두면 드리프트한다.

`core`는 단일 파일 유지를 권장한다(분할하면 tree-shaking 경계와 `sideEffects: false` 보장이
복잡해진다).

## 코드 컨벤션

### TypeScript

- **strict mode** 필수 (`tsconfig.json` 참조)
- `any` 타입 사용 금지. 불가피하면 주석으로 이유 명시
- Public API는 반드시 인터페이스/타입으로 명시
- optional 필드 추가 시 반드시 기본값 제공

### 포맷팅 (.prettierrc 기준)

- 2 스페이스, 세미콜론, 싱글쿼트, `printWidth: 100`, `trailingComma: 'all'`
- 변경 후 `pnpm format` 실행 필수 (CI에서 `format:check`로 검사)

### 주석

- Public 메서드에 JSDoc 한국어 주석 필수: `/** 설명 */`
- 복잡한 로직에는 인라인 주석 (한국어 가능)
- 주석·JSDoc·커밋 메시지는 **한국어 또는 영어만** 쓴다. 다른 언어(특히 일본어) 혼입 금지 (#1)

## 브랜치 전략

| 패턴                     | 용도                                  |
| ------------------------ | ------------------------------------- |
| `main`                   | 프로덕션 (push 시 npm publish 트리거) |
| `feat/기능명`            | 새 기능                               |
| `fix/버그명`             | 버그 수정                             |
| `ai/issue-{번호}-{설명}` | AI 자동 생성                          |
| `chore/작업명`           | 빌드/설정 변경                        |

main 직접 push 금지. 모든 변경은 PR로 머지하고, AI 생성 브랜치는 반드시 사람이 리뷰 후 머지한다.

## PR 가이드라인

제목 형식: `feat:` `fix:` `chore:` `docs:` `test:` `refactor:`

**AI 생성 PR**은 제목에 `[AI]` 접두어, `draft: true`, 본문 하단에
`🤖 Generated by jump-section AI Pipeline`.

## AI 파이프라인 거버넌스

저장소를 자동으로 수정하는 주체는 둘이다 — 로컬 Claude Code 세션과, GitHub Actions의
`anthropics/claude-code-action`(이슈 라벨·`@claude` 멘션·cron). 액션이 저장소를 체크아웃한 뒤
Claude Code를 돌리므로 **둘 다 같은 하네스**(이 문서 + `.claude/`)를 읽는다. 아래 규칙은 주체와
무관하게 적용된다.

여기에 **로컬 n8n**(`.n8n/`)이 하나 더 있는데 자동화 주체가 아니라 **관측 계층**이다.

| 자리                      | 담당           | 근거                                                |
| ------------------------- | -------------- | --------------------------------------------------- |
| 저장소를 바꾸는 일        | GitHub Actions | 체크아웃·토큰·러너가 이미 있고 공개 저장소라 무료다 |
| 관측·알림, GitHub 밖 연결 | 로컬 n8n       | 워크플로우마다 알림 스텝을 심지 않고 한 곳에서 본다 |

**새 자동화는 이 기준으로 고른다.** 저장소를 바꾸는 일이면 Actions에 워크플로우를 추가한다 — 서버를
세우지 않는다. 관측·알림이면 n8n에 둔다. **같은 일을 양쪽에 두지 않는다**(한때 이슈 생성이 양쪽에
있어 두 배로 생길 뻔했다).

n8n 쪽 원칙:

- **웹훅을 받지 않는다.** GitHub은 실패한 웹훅 전달을 재시도하지 않아서, 로컬 인스턴스가 꺼진 동안
  온 웹훅은 영구 유실된다. 폴링은 지연될 뿐 잃지 않고 터널·포트 개방도 필요 없다.
- **저장소 쓰기 권한을 주지 않는다.** 토큰은 Actions(Read) + Metadata(Read) 읽기 전용. 관측 계층이
  저장소를 바꿀 수 있으면 관측 계층이 아니다.

### 공통 규칙

- 보호 브랜치(`main`, `master`, `develop`, `release/**`)에는 어떤 주체도 직접 push할 수 없다.
  `.claude/settings.json`의 PreToolUse 훅(`block-protected-push.sh`)이 강제하고, 액션도 같은 훅을
  쓰므로 CI 에이전트에도 적용된다. 패턴은 `scripts/protected-branches.sh`(SSOT)에서 온다.
  회귀 테스트: `bash .claude/scripts/block-protected-push.test.sh`
- GitHub 토큰/PAT은 항상 fine-grained + 최소 권한. Administration, Workflows(write), Organization
  권한은 어떤 주체에게도 주지 않는다.
- 브랜치 규칙과 AI 생성 PR 규칙은 로컬 세션과 CI 자율 실행에 동일하게 적용된다.
- 변경이 불확실하면(이슈에 없는 설계 결정 등) 임의 판단하지 말고 PR/이슈 코멘트에 질문으로 남긴다.
- 자동화 경로(훅·워크플로우)를 추가·수정하면 **실패가 관측 가능한지**를 같은 변경에서 확인한다.
  실패는 비-0 종료나 알림으로 드러나야 하고(`curl`엔 `--fail`, 셸 훅엔 `bash -n`, 자율 워크플로우엔
  `if: failure()`), 머지 전 최소 한 번 실제 동작을 확인한다. 조용히 실패하는 자동화는 없는 것보다
  나쁘다 — 있다고 착각하게 만든다 (#3)
- **`claude-code-action` 워크플로우는 브랜치에서 테스트할 수 없다.** 액션이 워크플로우 파일 내용이
  기본 브랜치와 동일한지 검사하고, 다르면 Claude 실행을 건너뛴 채 **success 로 끝낸다**(`Skipping
action due to workflow validation`). `workflow_dispatch --ref <브랜치>` 도 마찬가지다. 그러니
  이 액션을 쓰는 워크플로우는 머지 후에 dispatch 로 확인하고, 실패하면 고쳐서 다시 머지한다 —
  브랜치에서 초록불이 떴다고 동작을 확인한 것이 아니다
- 같은 값이나 규칙을 두 곳 이상에서 강제할 때는 **복사하지 않는다.** 파일 하나를 SSOT로 두고
  source/참조한다. source할 수 없는 지점만 사본을 두고 SSOT를 가리키는 주석을 남긴다 (#4)
- 보안 가드(보호 브랜치, 서명 검증, origin 확인)는 **부분문자열 비교로 판정하지 않는다.** 구조를
  파싱해 앵커 비교하고 판정 단위를 명확히 한다(전체 명령이 아니라 명령 조각). 가드를 수정하면 회귀
  테스트를 함께 추가한다 (#2)

> `#숫자`는 `.claude/flywheel/learnings.md` 원장의 항목 번호다. 규칙이 왜 생겼는지는 거기 있다.

### 하네스 구조

| 계층     | 위치                            | 역할                                          |
| -------- | ------------------------------- | --------------------------------------------- |
| Rule     | `CLAUDE.md` (이 문서)           | 모든 계층이 따르는 최상위 정책                |
| Skill    | `.claude/skills/`               | 재사용 가능한 절차 정의                       |
| Agent    | `.claude/agents/`               | 도구 권한을 제한해 격리 실행하는 서브에이전트 |
| Hook     | `.claude/settings.json`         | 도구 사용 전후 자동 검증/후처리               |
| Flywheel | `.claude/flywheel/learnings.md` | 피드백을 규칙으로 되먹이는 자기 개선 루프     |

**이 5개가 자동화의 전부다.** GitHub Actions 워크플로우는 계층을 새로 만들지 않고 이 하네스를
호출하는 얇은 진입점이다 — `ai-implement.yml`은 `prompt`로 `/implement-issue` 스킬을 부르고,
체크아웃한 저장소의 `CLAUDE.md`·`.claude/skills/`·훅을 그대로 읽는다. 그래서 자동화용 규칙을 따로
쓰지 않는다.

슬래시 커맨드(`.claude/commands/`)는 쓰지 않는다 — 절차는 Skill로, 격리 실행은 Agent로 정의한다.

**규칙을 두 벌 두지 않는다.** 새 실행 주체가 생기면 그 주체가 이 하네스를 읽게 만든다 (#4)

### Flywheel

Rule/Skill/Agent/Hook은 정적이다. 같은 지적을 반복해서 받는데도 규칙이 그대로면 하네스는 개선되지
않는다. 이를 닫는 것이 Flywheel이다.

- **측정**: `.claude/scripts/post-pr-coverage.sh`가 PR 생성 시 커버리지·번들 사이즈·벤치마크를
  코멘트로 남긴다.
- **학습**: 피드백을 반영할 때마다 `.claude/flywheel/learnings.md`에 한 줄 append한다. 이는
  `pr-fix-conventions` 스킬의 **필수 마지막 단계**이며 로컬·CI 양쪽에 적용된다.
- **승격**: 같은 패턴이 2회 누적되면 이 문서나 해당 SKILL.md 본문의 규칙으로 올린다. 승격된 항목은
  원장에서 지우지 않는다.

기록 대상과 형식은 원장 문서 자체에 있다 — 여기에 옮겨 적지 않는다.

## 변경 시 필수 동반 작업

코드를 추가·수정할 때 **반드시 함께** 작업한다:

1. **테스트** — 새 기능/수정에 대한 케이스 추가 또는 기존 테스트 업데이트
2. **문서** — `docs/`의 해당 페이지(ko/en) 반영 (새 옵션·메서드·동작 변경 포함)
3. **타입** — Public API 변경 시 인터페이스/타입 정의 업데이트

> 테스트만, 문서만 수정하는 분리된 PR은 허용하지 않는다.

## 테스트 요구사항

```bash
pnpm test           # 전체 (vitest + jsdom)
pnpm test:coverage  # 커버리지 포함
```

- 모든 Public 메서드에 테스트 필수
- **새로 추가·수정한 코드는 80% 이상.** 전체 커버리지는 현재 40%대이므로 전체 수치로 판단하지 말고
  변경분 기준으로 본다 — 기존 미달을 새 PR의 책임으로 돌리지 않되, 새 코드가 그 수치를 더
  끌어내리게 하지도 않는다.
- 기존 테스트를 깨뜨리는 변경 금지
- `IntersectionObserver` 등 브라우저 API 목은 `packages/core/src/ScrollManager.test.ts`의 패턴을
  그대로 따른다 (여기에 사본을 두지 않는다)

## 빌드 시스템

```bash
pnpm build         # 전체 (turbo)
pnpm check-exports # exports 필드 검증 (arethetypeswrong)
```

- tsup (CJS + ESM + DTS 동시 출력)
- `sideEffects: false` 유지 (tree-shaking)
- 빌드 후 `pnpm check-exports` 통과 확인 필수
- 각 패키지는 **자기 `tsconfig.json`을 갖는다.** 루트로 폴백하면 형제 패키지의 `dist`를 컴파일
  대상으로 끌어와 병렬 빌드에서 간헐적으로 깨진다 (실제로 릴리스를 두 번 막았다)

## 하위 호환성 규칙

1. Export된 클래스/함수/타입의 시그니처 제거·변경 금지
2. 새 옵션은 반드시 optional + 기본값
3. `ScrollManager` 생성자 시그니처 변경 금지
4. react/vue/svelte 기존 훅 반환 타입 변경 금지

## 의존성 관리

- `@jump-section/core`: **zero dependency** 유지 필수
- 프레임워크 패키지: 해당 프레임워크만 peerDependency
- devDependency는 루트 `package.json`에 추가

## SSR 주의사항

- `typeof window === 'undefined'` 체크로 SSR 대응
- DOM API 사용 시 항상 null 체크
- 이벤트 리스너는 반드시 `destroy()`에서 제거
- scroll 이벤트에 `{ passive: true }` 필수
