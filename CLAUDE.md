# CLAUDE.md — jump-section AI Context

이 문서는 Claude AI가 jump-section 프로젝트를 자율적으로 수정할 때 필요한 컨텍스트입니다.

## 프로젝트 개요

**jump-section**은 웹 페이지에서 메뉴 클릭 시 해당 섹션으로 부드럽게 스크롤 이동하고,
스크롤 위치에 따라 활성 섹션을 자동으로 추적하는 TypeScript 라이브러리입니다.

- **저장소**: https://github.com/bae080311/jump-section
- **npm 패키지**: `@jump-section/core`, `@jump-section/react`, `@jump-section/vue`, `@jump-section/svelte`
- **패키지 매니저**: pnpm + turbo (모노레포)

## 핵심 아키텍처

`ScrollManager` 클래스 (`packages/core/src/ScrollManager.ts`) 가 모든 기능의 중심입니다.
프레임워크 패키지(react/vue/svelte)는 ScrollManager를 래핑한 얇은 어댑터 레이어입니다.

### 주요 API

```typescript
// 옵션
interface ScrollOptions {
  offset?: number;           // 고정 헤더용 오프셋 (px)
  behavior?: ScrollBehavior; // 'smooth' | 'auto' | 'instant'
  hash?: boolean;            // URL 해시 동기화
  root?: HTMLElement | null; // 커스텀 스크롤 컨테이너
  keyboard?: boolean;        // Alt+Arrow 키보드 네비게이션
}

// 핵심 메서드
manager.registerSection(id, element)
manager.unregisterSection(id)
manager.scrollTo(id): Promise<void>
manager.scrollToNext/Prev/First/Last(): Promise<void>
manager.onActiveChange(callback): unsubscribe  // meta: { previous, direction }
manager.onProgressChange(id, callback): unsubscribe
manager.disableSection(id) / enableSection(id)
manager.getSections(): string[]
manager.getActiveId(): string | null
manager.destroy()
```

## 코드 컨벤션

### TypeScript

- **strict mode** 필수 (`tsconfig.json` 참조)
- `any` 타입 사용 금지. 불가피한 경우 주석으로 이유 명시
- Public API는 반드시 TypeScript 인터페이스/타입으로 명시
- optional 필드 추가 시 반드시 기본값 제공

### 포맷팅 (.prettierrc 기준)

- 2 스페이스 들여쓰기, 세미콜론, 싱글쿼트
- `printWidth: 100`, `trailingComma: 'all'`
- 변경 후 `pnpm format` 실행 필수 (CI에서 검사)

### 주석

- Public 메서드에는 JSDoc 한국어 주석 필수: `/** 설명 */`
- 복잡한 로직에는 인라인 주석 (한국어 가능)
- 주석·JSDoc·커밋 메시지는 **한국어 또는 영어만** 사용한다. 다른 언어(특히 일본어) 혼입 금지
  — `.claude/flywheel/learnings.md` #1에서 승격된 규칙

## 브랜치 전략

| 패턴                     | 용도                                  |
| ------------------------ | ------------------------------------- |
| `main`                   | 프로덕션 (push 시 npm publish 트리거) |
| `feat/기능명`            | 새 기능 개발                          |
| `fix/버그명`             | 버그 수정                             |
| `ai/issue-{번호}-{설명}` | AI 자동 생성 브랜치                   |
| `chore/작업명`           | 빌드/설정 변경                        |

- main 브랜치 직접 push 금지
- 모든 변경은 PR을 통해 머지
- AI 생성 브랜치는 반드시 사람이 리뷰 후 머지

## PR 가이드라인

### 제목 형식

```
feat: 새 기능 추가
fix: 버그 수정
chore: 빌드/설정 변경
docs: 문서 수정
test: 테스트 추가/수정
refactor: 리팩토링
```

### AI 생성 PR 규칙

- 제목에 `[AI]` 접두어 필수
- `draft: true` 로 생성
- 본문 하단에 `🤖 Generated by jump-section AI Pipeline` 명시

## AI 파이프라인 거버넌스

이 저장소를 자동으로 수정하는 주체는 두 가지다: Claude Code 세션(사람이 로컬에서 직접 실행)과
GitHub Actions의 `anthropics/claude-code-action`(이슈 라벨·`@claude` 멘션·cron으로 자율 실행).
둘 다 **같은 하네스**(이 문서 + `.claude/`)를 읽는다 — 액션이 저장소를 체크아웃한 뒤 Claude Code를
돌리기 때문이다. 아래 규칙은 **주체와 무관하게 동일하게 적용**된다.

여기에 **로컬 n8n**(`.n8n/`)이 하나 더 있는데, 자동화 주체가 아니라 **관측 계층**이다. 저장소를
바꾸지 않고 GitHub API를 폴링해 실패를 Discord로 알린다.

| 자리                      | 담당           | 근거                                                         |
| ------------------------- | -------------- | ------------------------------------------------------------ |
| 저장소를 바꾸는 일        | GitHub Actions | 체크아웃·토큰·러너가 이미 있고, 공개 저장소라 무료다         |
| 관측·알림, GitHub 밖 연결 | 로컬 n8n       | 워크플로우마다 알림 스텝을 심지 않고 한 곳에서 전부 커버한다 |

**새 자동화를 추가할 때 이 기준으로 고른다.** GitHub 이벤트가 저장소를 바꾸는 일이면 Actions에
워크플로우를 추가한다 — 서버를 세우지 않는다. 관측·알림이거나 GitHub 밖 서비스를 엮는 일이면
n8n에 둔다. 같은 일을 양쪽에 두면 안 된다: 한때 이슈 생성이 n8n과 Actions에 모두 있어 이슈가 두 배로
생길 뻔했다.

이전에는 n8n(Fly.io) + Hermes Agent(Fly.io) 2개 앱이 상주하며 저장소까지 바꿨다. always-on 비용을
냈고, `.hermes/skills/`가 `.claude/skills/`를 복제했고, 실패가 조용했다(파이프라인이 한 번도 동작한
적이 없는데 4개월간 아무도 몰랐다). 그래서 저장소를 바꾸는 역할은 Actions로 옮기고, n8n은 관측만
남겨 로컬로 내렸다. Actions 인증은 `CLAUDE_CODE_OAUTH_TOKEN`(구독 기반, API 과금 없음)을 쓴다.

n8n 쪽 원칙 두 가지:

- **웹훅을 받지 않는다.** 모든 워크플로우가 GitHub API를 폴링한다. GitHub은 실패한 웹훅 전달을
  재시도하지 않으므로, 로컬 인스턴스가 꺼져 있는 동안 온 웹훅은 영구히 유실된다. 폴링은 다운타임이
  알림을 늦추기만 하고 잃지 않으며, outbound만 쓰므로 터널도 포트 개방도 필요 없다.
- **저장소에 쓰기 권한을 주지 않는다.** n8n의 `GITHUB_TOKEN`은 Actions(Read) + Metadata(Read)
  읽기 전용이다. 관측 계층이 저장소를 바꿀 수 있으면 관측 계층이 아니다.

### 공통 규칙

- 보호 브랜치(`main`, `master`, `develop`, `release/**`)에는 어떤 자동화 주체도 직접 push할 수 없다.
  `.claude/settings.json`의 PreToolUse 훅(`.claude/scripts/block-protected-push.sh`)이 강제하며,
  액션도 같은 저장소의 같은 훅을 쓰기 때문에 CI에서 도는 에이전트에도 그대로 적용된다.
  패턴은 `scripts/protected-branches.sh`(SSOT)에서 온다. 회귀 테스트:
  `bash .claude/scripts/block-protected-push.test.sh`
- GitHub 토큰/PAT은 항상 fine-grained + 최소 권한으로 발급한다. Administration, Workflows(write),
  Organization 권한은 어떤 자동화 주체에게도 부여하지 않는다.
- `ai/issue-{번호}-{설명}` 브랜치 규칙과 "AI 생성 PR 규칙"(`[AI]` 접두어, `draft: true`,
  `🤖 Generated by jump-section AI Pipeline`)은 로컬 Claude Code 세션이 만드는 PR뿐 아니라
  GitHub Actions에서 자율 실행된 에이전트가 만드는 PR/커밋에도 동일하게 적용된다.
- 변경이 불확실하면(이슈에 명시되지 않은 설계 결정 등) 임의로 판단하지 말고 PR/이슈 코멘트에 질문으로
  남긴다.
- 자동화 경로(훅·GitHub Actions 워크플로우)를 추가하거나 수정하면 **실패가 관측 가능한지**를 같은
  변경에서 확인한다. 실패는 비-0 종료나 알림으로 드러나야 하고(`curl`에는 `--fail`, 셸 훅에는
  `bash -n`, 자율 워크플로우에는 `if: failure()` 알림 스텝), 머지 전에 최소 한 번 실제로 동작하는
  것을 확인한다(`workflow_dispatch`를 달아두면 기다리지 않고 확인할 수 있다).
  조용히 실패하는 자동화는 없는 것보다 나쁘다 — 있다고 착각하게 만든다
  (`.claude/flywheel/learnings.md` #3에서 승격된 규칙).
- 같은 값이나 규칙을 두 곳 이상에서 강제할 때는 **복사하지 않는다.** 파일 하나를 SSOT로 두고
  source/참조한다(보호 브랜치 패턴 → `scripts/protected-branches.sh`). 셸을 source할 수 없는
  지점만 사본을 두고, SSOT 파일을 가리키는 주석을 남긴다
  (`.claude/flywheel/learnings.md` #4에서 승격된 규칙).
- 보안 가드(보호 브랜치, 서명 검증, origin 확인)는 **부분문자열 비교로 판정하지 않는다.** 구조를
  파싱해 앵커 비교하고, 판정 단위를 명확히 한다(전체 명령이 아니라 명령 조각). 가드를 수정하면 회귀
  테스트 케이스를 함께 추가한다 (`.claude/flywheel/learnings.md` #2).

### 하네스 구조 (Claude Code 쪽)

이 저장소의 Claude Code 설정은 5개 계층으로 구성된다:

| 계층     | 위치                            | 역할                                                      |
| -------- | ------------------------------- | --------------------------------------------------------- |
| Rule     | `CLAUDE.md` (이 문서)           | 모든 계층이 따르는 최상위 정책                            |
| Skill    | `.claude/skills/`               | 재사용 가능한 절차 정의(이슈 구현, PR 수정 컨벤션 등)     |
| Agent    | `.claude/agents/`               | 특정 역할에 도구 권한을 제한해 격리 실행하는 서브에이전트 |
| Hook     | `.claude/settings.json`         | 도구 사용 전후로 자동 실행되는 검증/후처리                |
| Flywheel | `.claude/flywheel/learnings.md` | 리뷰 피드백을 규칙으로 되먹이는 자기 개선 루프            |

슬래시 커맨드(`.claude/commands/`)는 쓰지 않는다 — 절차는 Skill로, 격리 실행이 필요한 역할은 Agent로
정의한다.

**이 5개 계층이 자동화의 전부다.** GitHub Actions 워크플로우는 계층을 새로 만들지 않고 위 하네스를
호출하는 얇은 진입점일 뿐이다 — `.github/workflows/ai-implement.yml`은 `prompt`로
`/implement-issue` 스킬을 호출하고, 액션이 체크아웃한 저장소의 `CLAUDE.md`·`.claude/skills/`·
`.claude/settings.json` 훅을 그대로 읽는다. 그래서 자동화용 규칙을 따로 쓸 필요가 없다.

이전에는 `.hermes/skills/`가 `.claude/skills/`를 복제하고 수동 동기화에 의존했다. 실제로 어긋났고
(`.claude/flywheel/learnings.md` #4), 그래서 한 벌만 남겼다. **규칙을 두 벌 두지 않는다** —
새 실행 주체가 생기면 그 주체가 이 하네스를 읽게 만든다.

### Flywheel — 자기 개선 루프

Rule/Skill/Agent/Hook은 정적이다. 리뷰에서 같은 지적을 반복해서 받는데도 규칙이 그대로면 하네스는
개선되지 않는다. 이를 닫아주는 것이 Flywheel 계층이다.

```
Rule → Skill/Agent → 구현 → Hook(측정) → Review(피드백) → Learn(원장) ─┐
  ↑                                                                     │
  └───────────────────── 같은 패턴 2회 누적 시 승격 ─────────────────────┘
```

- **측정**: `.claude/scripts/post-pr-coverage.sh`가 PR 생성 시 커버리지/번들 사이즈/벤치마크를
  코멘트로 남긴다.
- **학습**: 리뷰 피드백을 반영할 때마다 `.claude/flywheel/learnings.md` 원장에 한 줄 append한다.
  이는 `pr-fix-conventions` 스킬의 **필수 마지막 단계**이며, 로컬 세션과 CI 자율 실행 양쪽에
  동일하게 적용된다.
- **승격**: 같은 패턴이 2회 누적되면 이 문서 또는 해당 SKILL.md 본문의 규칙으로 올린다. 승격된
  항목은 원장에서 지우지 않는다.

기록 대상은 컨벤션 위반·반복 가능한 판단 실수·하네스 자체의 빈틈이다. 일회성 오타나 그 PR에만
해당하는 로직 실수는 기록하지 않는다 — 원장이 길어지면 아무도 안 읽는다.

## 변경 시 필수 동반 작업

코드를 추가하거나 수정할 때 **반드시 아래 세 가지를 함께 작업**해야 한다:

1. **테스트** — 새 기능/수정에 대한 테스트 케이스 추가 또는 기존 테스트 업데이트
2. **문서** — `docs/` 의 해당 페이지(ko/en) 내용 반영 (새 옵션, 메서드, 동작 변경 포함)
3. **타입** — Public API 변경 시 `ScrollOptions` 등 인터페이스/타입 정의 업데이트

> 테스트만 추가하거나 문서만 수정하는 식의 분리된 PR은 허용하지 않는다.
> 기능 PR에는 테스트 + 문서가 반드시 포함되어야 한다.

## 테스트 요구사항

```bash
pnpm test          # 전체 테스트 (vitest + jsdom)
pnpm test --watch  # 감시 모드
```

- 모든 Public 메서드에 대한 테스트 필수
- 새 기능: 80% 이상 커버리지
- 기존 테스트를 깨뜨리는 변경 금지

### IntersectionObserver Mock 패턴

```typescript
class IntersectionObserverMock implements IntersectionObserver {
  constructor(
    public callback: IntersectionObserverCallback,
    public options?: IntersectionObserverInit,
  ) {}
  observe = vi.fn();
  unobserve = vi.fn();
  disconnect = vi.fn();
  takeRecords = (): IntersectionObserverEntry[] => [];
  readonly root = null;
  readonly rootMargin = '';
  readonly thresholds = [];
}
vi.stubGlobal('IntersectionObserver', IntersectionObserverMock);
```

## 빌드 시스템

```bash
pnpm build         # 전체 빌드 (turbo)
pnpm check-exports # exports 필드 검증 (arethetypeswrong)
```

- 빌드 도구: tsup (CJS + ESM + DTS 동시 출력)
- `sideEffects: false` 유지 (tree-shaking 지원)
- 빌드 후 `pnpm check-exports` 통과 확인 필수

## 하위 호환성 규칙

1. Export된 클래스/함수/타입의 시그니처 제거/변경 금지
2. 새 옵션 추가 시 반드시 optional + 기본값
3. ScrollManager 생성자 시그니처 변경 금지
4. react/vue/svelte 패키지의 기존 훅 반환 타입 변경 금지

## 의존성 관리

- `@jump-section/core`: **zero dependency** 유지 필수
- 프레임워크 패키지: 해당 프레임워크만 peerDependency
- devDependency 추가 시 루트 `package.json`에 추가

## SSR 주의사항

- `typeof window === 'undefined'` 체크로 SSR 환경 대응
- DOM API 직접 사용 시 항상 null 체크
- 이벤트 리스너는 반드시 `destroy()` 에서 제거
- scroll 이벤트에 `{ passive: true }` 옵션 필수

## 파일 구조 (변경 시 참고)

```
packages/
  core/src/
    ScrollManager.ts   # 핵심 클래스 (단일 파일 유지 권장)
    index.ts           # re-export만
  react/src/
    ScrollSectionContext.tsx  # Provider + Context
    useScrollSection.ts       # useScrollSection, useScrollProgress
    Section.tsx               # forwardRef 래퍼 컴포넌트
    index.ts
  vue/src/
    useScrollSection.ts       # provideScrollManager, useScrollSection, useScrollProgress
    index.ts
  svelte/src/
    useScrollSection.ts       # provideScrollManager, useScrollSection, useScrollProgress
    index.ts
```
